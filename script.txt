const MY_TON_WALLET = "UQBT3bJqKbJMgQpWc4I5zPc15ep1eFxwuo-ocyTQ6WyJKV8q"; 
const MY_MANIFEST_URL = "https://britva-krot-tg.github.io/Telegram-alc/tonconnect-manifest.json";
const BACKEND_URL = "http://localhost:8000"; // Адрес твоего Python-сервера

const tg = window.Telegram.WebApp;
if (tg) {
    tg.expand();
    if (tg.ready) tg.ready();
}
const myTelegramId = (tg && tg.initDataUnsafe && tg.initDataUnsafe.user) ? tg.initDataUnsafe.user.id : 123456789;